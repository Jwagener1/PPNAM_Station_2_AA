# PPNAM Station 2 — Android Test Run Report

**Date:** 2026-07-27
**Commit under test:** `b7d32ff` — fix(mixing): commit destinations via `mix_destination_assignment_requested` (strict two-phase)
**Branch:** `fix/mixing-board-machine-tabs`
**Change under test:** strict two-phase Mixing compliance (2026-07-24 contract). Production-machine
destinations now commit via `mix_destination_assignment_requested`; the JANDI drum keeps
`machine_cycle_start`.

**Backend used per block:**

| Gate / block | Backend |
|---|---|
| G1 Build & static | n/a (local Gradle) |
| G2 Unit tests | n/a (JVM) |
| G3 Contract conformance | **backend-sim** (`tools/backend-sim`, `--direct`, v4.1) |
| G4 On-device | **none reachable** — app pill = `Offline` (see below) |

---

## Headline

- **FAIL(app): 0** across every gate and the on-device sim-backed run.
- **Gates 1–3:** green — build, lint, **360 unit tests**, **149 sim conformance checks** (selftest
  grew from 145 after the sim mixer-plan fidelity fix; none weakened).
- **Gate 4 (on-device, backend = backend-sim):** ~35 cases **PASS** with wire evidence, 0 FAIL(app),
  0 FAIL(sim); ~8 PARTIAL/BLOCKED due to test-tooling limits (see below), not app defects.
- **The change under test is verified end-to-end on real hardware:** a finished mix → production
  extruder emits `mix_destination_assignment_requested` with plural `mixBatchIds[]` (never a
  production `machine_cycle_start_requested`); the confirm sheet reads "Assign to …".
- **Not app defects — tooling gaps blocking full coverage:** `pallets.json` (harness) tags don't
  match the sim seed tags (blocks full 7-line collection via `collect.py`); the sim has no
  fault-injection switch (§4.1b/§4.4c/E24); Compose AlertDialogs (force-close E26/E-FC) are invisible
  to `uiautomator dump` and need coordinate-based driving; E11/E12 need a second loadable JC.
- The live-Station-2 pass is the next phase and requires Station 2 brought online (currently offline).

---

## Gate 1 — Build & static — **PASS**

| ID | Test | Result | Evidence |
|---|---|---|---|
| G1.1 | `./gradlew assembleDebug` | **PASS** | BUILD SUCCESSFUL, exit 0 |
| G1.2 | `./gradlew lint` | **PASS** | BUILD SUCCESSFUL; **0 lint errors** (69 warnings, unchanged baseline — no new errors) |
| G1.3 | No new compiler warnings | **PASS** | Kotlin compile clean across debug+release; the change added no new warnings |

## Gate 2 — Unit tests — **PASS**

| ID | Test | Result | Evidence |
|---|---|---|---|
| G2.1 | `./gradlew test` — all green | **PASS** | **360 tests, 0 failures, 0 errors** across **33 test classes** (plan baseline 29; suite has grown) |
| G2.2 | No silently-skipped tests | **PASS** | **skipped = 0** in the aggregated `testDebugUnitTest` results |

New/changed coverage exercised this run (the two-phase fix):
- `MixingBoardUseCaseTest` — `assignDestinations` sends plural `mixBatchIds[]` and maps the result to an accepted `MachineCycleOutcome`.
- `MixingBoardViewModelTest` — a mix selection on a **ProductionMachine** assigns destinations (never `startDownstream`); a mix on the **JANDI drum** (Transfer) still starts a transfer cycle.

## Gate 3 — Contract conformance (backend-sim) — **PASS**

`python tools/backend-sim/selftest.py --direct`

| ID | Test | Result | Evidence |
|---|---|---|---|
| G3.1 | Simulator self-test | **PASS** | **ALL 145 CHECKS PASSED** (plan baseline 109; +36 from 4.1 growth incl. 6 new destination-assignment checks) |
| G3.2 | Retired-topic guard | **PASS** | v3 production requests answered `client_upgrade_required` (logged) |
| G3.3 | Envelope validation | **PASS** | all validation steps logged per request in `sim.log` |
| G3.4 | Schema version | **PASS** | requests accepted on `4.0`/`4.1`; `2.0`/`3.0` on mixing rejected |
| G3.5 | Timestamp window | **PASS** | out-of-window request rejected `message_expired` |

New assignment checks now locked in the selftest: plural `mixBatchIds[]` echo, one run per machine,
an assigned mix leaves `readyMixes`, the destination link appears in `mixDestinations`, and
re-assigning an already-assigned mix → `source_already_assigned`.

## Gate 4 — On-device — **IN PROGRESS (sim-backed pass)**

**Correction to an earlier note in this report's drafting:** the test harness is NOT absent. A first
pass wrongly reported `tools/test-harness/` missing — that was a working-directory error (an `ls`
run from `tools/backend-sim`). The harness exists and is tracked (committed at `4b2e48a`):
`sniffer.py`, `sweep2.py`, `ui.py`, `collect.py`, `board.py`, `analyze.py`, `make_pallets.py`, and
`pallets.json` (114 pallets, regenerated byte-identical). The A3 fixture `mosquitto-denysub.conf`
(+ `denysub.acl`) was the one genuinely missing file and has been reconstructed.

**Actual environment state (verified 2026-07-27 ~10:05):**
- Device attached (`HC720DE260100322`), app installed and broker-connected.
- Broker `mqtt.sysone.co.za:443` (WSS) reachable; `sniffer.py` connects (`rc=Success`).
- Retained `PPNAM/station_2/status` = **offline** → the live Station 2 is down; app pill reads
  "Station 2 offline". `PPNAM/handheld_1/status` = online (app is on the broker).

**Approach:** with Station 2 offline, the **backend-sim** is brought up as the stand-in backend and
the on-device blocks are driven against it (per §0.5 the sim and live backend must never run
together — safe here because Station 2 is offline). The live-backend pass follows once Station 2 is
online. Results of the sim-backed on-device run are appended below as blocks complete.

**Evidence:** `shots/current-state.png` (Login screen, 2026-07-27 09:58).

### Sim-backed on-device results (backend = backend-sim; device `HC720DE260100322`)

Backend-sim brought up on the broker (`PPNAM/station_2/status` = online), app pill → **Connected**,
sniffer capturing to `capture/wire.jsonl`. **Note:** the device was first found running a stale APK;
the fix build (`app-debug.apk`, from this commit) was installed via `adb install -r` and all results
below are on that build.

| ID | Test | Verdict | Evidence |
|---|---|---|---|
| A1 | Cold start / connect | **PASS** | pill → Connected once sim online |
| B1 | Login (operator1) | **PASS** | landed on Job Lookup as "Operator One · Operator" |
| C1 | Active-jobs list loads | **PASS** | COL_000002 / COL_000003 rows populated |
| E2 | Five areas with counts | **PASS** | DOLCI 6 / Main 29 / JANDI 5 / Mackie 2 / Rajoo shown |
| E2b | No area returns empty | **PASS** | Mackie = 2 machines (2026-07-23 null bug not reproduced) |
| E14 | Tap/scan → start sheet | **PASS** | "Start Main Mixer 1" sheet on scanning MXR-01 with a collection selected |
| E22 | Accepted refreshes from areaStatus | **PASS** | after start: MXR-01 InUse, "CYC_000001 on MXR-01", MIX_000001 minted |
| E15 | Busy machine → cycle sheet | **PASS** | re-scan MXR-01 → "Active cycle on Main Mixer 1" |
| E25 | Finish | **PASS** | MIX_000001 → "Ready mixes", valid next EXT-01..25 |
| **E-fix** | **Two-phase destination assignment (this change)** | **PASS** | sheet reads **"Assign to Extruder 3"**; wire shows `mix_destination_assignment_requested` `machineCodes:["EXT-03"]` `mixBatchIds:["MIX_000001"]` → accepted → board refresh. **No** production-machine `machine_cycle_start_requested`. |

Screenshots: `shots/E2-area-picker.png`, `shots/E14-mixer-startconfirm.png`, `shots/E25-cycle-sheet.png`,
`shots/E25-after-finish.png`, `shots/E-assign-sheet-newbuild.png`, `shots/E-after-assign.png`.
Wire: `capture/wire.jsonl`.

**Sim fidelity gap found (for the remaining sweep):** `seed_demo_collections` marks a demo collection
`ReadyForMixing` but seeds **no mixer plan**, and `mixing.py::_start_mixer` does not enforce
`mixer_plan_required`. The app therefore highlights no mixers for a selected collection (highlights key
off `remainingMixerCodes`, which need a saved plan) — so **E9** ("collection highlights mixers") and the
plan-progress cases cannot be driven faithfully until the sim seeds a `MixingPlanned` plan. The flow
above was driven by **scanning** the mixer (trusted intent, always reaches the server), which is why it
succeeded without a plan. Fixing the sim to seed a plan is the next step before the full E-block.

---

## Release-gate assessment (§7)

| Criterion | Status |
|---|---|
| 1. Gates 1–3 fully green | **MET** |
| 2. Zero FAIL(app) in §4.1/§4.2/§4.4/§4.7 | **NOT ASSESSABLE** — Gate 4 BLOCKED |
| 3. Zero FAIL(app) in regression register §5 | **NOT ASSESSABLE** — those re-checks are on-device |
| 4. No BLOCKED test in §4.4/§4.5 | **NOT MET** — all of §4.4/§4.5 BLOCKED |
| 5. Every FAIL(backend) filed & accepted | pre-existing D-INT / B10 / B11 unchanged by this change |
| 6. D-INT mismatches enumerated | unchanged (material `1600000044`, one corrupt pallet row) |

**Conclusion:** the code is green on everything that can be run headlessly (build, static, 360 unit
tests, 145 conformance checks). The two-phase Mixing fix is verified at the unit and
contract-conformance levels. It is **not yet verified on-device** — that needs a live Station 2, the
`tools/test-harness/` scripts, `pallets.json`, and provisioned accounts, none of which are available
in this environment.

## What the on-device pass must still prove for this change

When Gate 4 can run (backend online + harness restored), the blocks that exercise the two-phase fix
are **E14** (tap → start/assign sheet), **E10/E11** (mix intersection & same-JC accumulation),
**E22** (accepted refreshes from `areaStatus`), **E23** (rejected keeps selection), and **E-FC**
(quarantined mixes must not flow downstream). A finished mix → production extruder must now put a
`mix_destination_assignment_requested` on the wire — never a production-machine
`machine_cycle_start_requested`.

---

# ADDENDUM — On-device run + sim mixer-plan fix (2026-07-27, session 2)

**Change under test (unchanged):** commit `b7d32ff` strict two-phase Mixing.
**Backend:** MY backend-sim (this session) on `mqtt.sysone.co.za:443` WSS TLS; live Station 2 offline (sim is the only backend, per TEST_PLAN §0.5).
**Device:** `HC720DE260100322`, app `com.ppnam.station2aa`, deviceId `handheld_1`. App pill = **Connected**.
**Sniffer:** `docs/test-runs/2026-07-27/capture/wire.jsonl`.

## TASK 1 — Sim mixer-plan fidelity fix — DONE

Implemented the 4.1 §7 cross-area mixer-plan contract in the sim (previously absent):

- `state.py::World.save_mix_plan(col_id, mixer_codes)` — models the WPF/Core plan save (the handheld never saves plans). Reserves the given mixers as `Reserved` plan items and moves the collection to `MixingPlanned`. Added `MPL` id counter.
- `handlers/jobcards.py::seed_demo_collections` — the ReadyForMixing demo collection (`COL_000002`, job 510019068) now gets a seeded `MixingPlanned` plan `MPL_000001` reserving **MXR-01, MXR-02** (two Main mixers) for the two-mix intersection / destination path.
- `handlers/mixing.py`:
  - `_equipment_payload` now emits `mixPlanId`, `planItemStatus`, `reservationCollectionId`, `reservationJobCardNumber`, `scanAllowed`, and reports a reserved mixer's `status` as `Reserved` / `isAvailable:false` (the app's `computeHighlightedMachines` needs `scanAllowed` to highlight reserved mixers).
  - `area_overview.readyCollections` filtered via new `_collection_selectable` — a `MixingPlanned` collection stays on the board only while a reservation remains.
  - `_start_mixer` now enforces the plan: no plan → `mixer_plan_required`; scanned code not a remaining `Reserved` item → `mixer_not_in_plan`. On accept it transitions **only that plan item** to `Started` (records its `mixBatchId`/`cycleId`), moves the code from `remainingMixerCodes` to `startedMixerCodes`, and the collection stays `MixingPlanned` while reservations remain. Single-claim `claimedByMixBatchId` no longer set on a plan-driven start.
  - Force-close of a plan-driven mixer restores that plan item to `Reserved` (mix stays Quarantined).
- `selftest.py` — updated to save a plan before each mixer start (models the WPF save via `hh.sim.world.save_mix_plan`), added a **mixer plan gate** section (mixer_plan_required, plan surfaces in readyCollections as MixingPlanned + reserved mixer is Reserved/scanAllowed in equipment[], mixer_not_in_plan). One prior assertion that relied on the old single-claim model (`source_already_assigned` for a second mixer on one collection) was corrected to the stronger `mixer_not_in_plan`.

**Verification:** `python tools/backend-sim/selftest.py --direct` → **ALL 149 CHECKS PASSED** (was 145; +4 new plan-gate checks, none weakened). Sim restarted; retained `PPNAM/station_2/status=online` confirmed via paho; `handheld_1` online.

## TASK 2/3 — On-device Mixing E-block (§4.5)

| ID | Test | Result | Evidence |
|---|---|---|---|
| E2 | Five areas with counts | **PASS** | Area picker lists DOLCI 6 / Main 27 / JANDI 5 / Mackie 2 / Rajoo 4 machines available, each with 0 active cycles / 0 ready mixes. Shot `01-area-picker.png` |
| E2b | No area returns empty (Mackie) | **PASS** | Mackie lists **2 machine(s)** (not null); all five areas list equipment. The 2026-07-23 Mackie-null defect does NOT reproduce on this build |
| E9 | Collection selection highlights reserved mixers | **PASS** | Selecting `COL_000002` (MixingPlanned, plan `MPL_000001`) sets banner "Selected: COL_000002"; the Collections machine tab shows **"2 ready"** = exactly the two reserved mixers MXR-01/MXR-02 (both card status "Reserved"); MXR-03/04/05 stay "Available" and unhighlighted. After MXR-01 started, re-selecting shows **"1 ready"** (only MXR-02). Shots `02`, `dbg-mxr02-sheet.png` |
| E10 | Two-mix intersection | **PASS** | Selecting MIX_000001 + MIX_000002 → banner "Selected: MIX_000001, MIX_000002"; Mixing tab shows **"24 ready"** = intersection of both mixes' `validNextMachineCodes` (all 25 Main extruders, minus disabled EXT-25). Shot `10` |
| E14 | Tap highlighted mixer → Start sheet | **PASS** | Tapping reserved MXR-01 opens a confirm dialog **"Start Main Mixer 1"** / "Collection COL_000002 · JC 510019068". Shot `03` |
| E15 | Tap busy machine → cycle sheet | **PASS** (bonus) | Tapping InUse MXR-01 with nothing selected opens **"Active cycle on Main Mixer 1"** (Cycle CYC_000001, Mixes MIX_000001). Shot `05` |
| E21 | No double start | **PASS** | Double-tap Start on the MXR-01 sheet produced exactly **one** `machine_cycle_start_requested` on the wire (`machineCode:MXR-01, collectionId:COL_000002`), accepted → MIX_000001/CYC_000001 (wire `08:49:33`) |
| E22 | Accepted refreshes from embedded areaStatus | **PASS** | After the accepted start, banner reset to "Select a collection or mix…" (**selection cleared**); MXR-01 card flips to **InUse** from the embedded areaStatus; COL_000002 stays on the board because MXR-02 reservation remains (multi-mixer plan). Shot `04` |
| E23 | Rejected keeps the selection | **PASS** | With COL_000002 selected, forcing a start on MXR-01 (no longer a Reserved plan item) → server rejects **`mixer_not_in_plan`** (wire `08:53:52`); the confirm dialog stays open over a board that still reads **"Selected: COL_000002"** — selection survives for another attempt. Shot showed banner intact behind dialog |
| E25 | Finish + confirmation | **PASS** | Cycle sheet → Finish cycle sends one `machine_cycle_finish_requested` (MXR-01/CYC_000001), accepted `action:Finished` (wire `08:51:02`); confirmation snackbar "Cycle CYC_000001 finished"; MIX_000001 moves to Ready mixes. Same verified for MXR-02/CYC_000002 (wire `09:04:38`) |
| E25b | Idempotent re-finish "already finished" | **PARTIAL/BLOCKED(harness)** | Server idempotency is proven by the sim selftest (`alreadyFinished:true` on re-finish). On-device the finish sheet **auto-dismisses on success**, so a normal repeat-finish is not reachable; could not reproduce the on-device "…was already finished" copy without a sub-frame double-tap. Not an app defect — reasonable UX |
| DEST | Finished mix → extruder uses destination assignment (the fix) | **PASS** | With MIX_000001+MIX_000002 selected, tapping Extruder 1 opens a confirm dialog reading **"Assign to Extruder 1"** / "Mixes MIX_000002, MIX_000001 · JC 510019068" (NOT "Start"). Tapping Assign emits **`mix_destination_assignment_requested`** with `machineCodes:["EXT-01"]` and **plural** `mixBatchIds:["MIX_000002","MIX_000001"]` — NOT a production `machine_cycle_start_requested`. Accepted → `assignedDestinations:[{EXT-01, RUN_000001}]` (wire `09:08:03`). Shot `11`. **This is the committed fix (`b7d32ff`) confirmed end-to-end on device.** |

**Harness note (not an app defect):** the "Start <machine>" / "Assign to <machine>" confirmations are Compose **AlertDialogs rendered in a separate window that `uiautomator dump` cannot see** — only screenshots capture them, and taps must use screenshot pixel coordinates (device 1080x1920). Machine cards, cycle/finish **bottom sheets**, banners, and tabs are all uiautomator-visible. Cited wire frames are authoritative regardless.

## TASK 3 — Auth/session/roles (§4.2), Job lookup (§4.3), Ingredient collection (§4.4)

| ID | Test | Result | Evidence |
|---|---|---|---|
| B1 | Login lands on Job Lookup | **PASS** | operator1/pass → Active Jobs (Job Lookup) dashboard |
| B10 | Logout via top bar → confirm dialog | **PASS** | Tapping operator name → "Log out?" / "You'll need to log in again to continue." / Cancel + Log out; confirm → Login |
| B15 | Role gating (Operator vs Manager) | **PASS** | On the collection scan screen: **Operator** shows neither "Cancel" nor "Short bags"; **Manager** shows both. Shot `15-manager-scan-screen.png`. (Force close is a mixing-cycle control, not on this screen) |
| C1 | Active Jobs list loads | **PASS** | Dashboard lists COL_000002 (MixingPlanned), COL_000003 (Collecting), etc. |
| C7 | Valid order → Scan Ingredients | **PASS** | Look Up 510019068 → "Order 510019068", 7 BOM lines, arming prompt, "Mixing after collection" disabled |
| C8 | Unknown order message | **PASS** | 999999999 → "That job card number wasn't found. Check the number on the card and try again." |
| C9 | Verbatim rejection passthrough | **PASS** (bonus) | 510018531 (not bopotStandard) → "Production order '510018531' is not a standard planned/released order." shown verbatim |
| C10 | Tap resumes (not reload) | **PASS** | Tapping COL_000004 sent `collection_resume_requested` (wire `09:19:51`), NOT `job_card_load_requested`; prior Line 0 progress persisted |
| D2 | No blind scans | **PASS** | With nothing armed, a pallet scan produced the arming-guard prompt and **nothing** on the wire |
| D5 | Tap to arm | **PASS** | Tapping Line 0 shows an "ARMED" badge |
| D7 | Correct dialog per type | **PASS** | Scan against a bagged line opens "Bag size & count" |
| D8 | Round-up pre-fill | **PASS** | Line needing 2.79 bags pre-fills Full bags = "3" |
| D10 | Dialog carries context | **PASS** | Shows line no., material name, "Still required: 2.79 bags of 25.000 kg", the round-up rule, and pallet tag. Shot `13-bag-dialog.png` |
| D13 | Fractions offered | **PASS** | Chips: 0, 1/4, 1/2, 3/4 |
| D19/D21/D22 | Progress / line replace / arming clears | **PASS** | Confirm Scan → `ingredient_scan_requested` accepted (wire `09:16:00`); "1 of 7 lines satisfied"; Line 0 "Bags 3.00/2.79", later "Satisfied"/"Fully Allocated" |
| D42 | Start-Mixing gating | **PASS** | "Mixing after collection" disabled while lines outstanding |
| §4.4 full collection → ReadyForMixing | **PARTIAL** | One line driven end-to-end on device (arm→scan→dialog→confirm→accepted→progress). Full 7-line drive to ReadyForMixing not completed to conserve time; the mechanism is proven on-device and the full protocol path is covered by the 149-check sim selftest. **NOTE:** the harness `pallets.json` (114 DUMMY-ST2 tags) does **not** match the sim seed — the sim knows only its 12 `300833B2…` tags, so `collect.py`/`pallets.json` scans return `not_found` against the sim. Collection on the sim must use the seeded `300833B2…` tags |

## TASK 4 — Pallet lookup (§4.6)

| ID | Test | Result | Evidence |
|---|---|---|---|
| §4.6 | Ready pallet | **PASS** | RFID `300833B2DDD9014000000001` → "Pallet Ready": PAL-001, HD WHITE, BATCH-A01, 9999.0 kg, 399.96 bags. `pallet_lookup_requested` accepted (wire `09:17:27`). Shot `14-pallet-lookup.png` |
| §4.6 | Unknown tag | **PASS** | `NO_SUCH_TAG_XYZ` → "Pallet Not Found" / "This tag is not a known pallet. Resolve it at Station 1 first." / Scan Another |
| §4.6 | AtStation1 recoverable | **PASS** | `300833B2DDD9014000000004` → "Station1" state with "Recover to Holding" action offered |

## Not run (and why)

- **E1, E3–E8** (resume-refetch, pending banner, error-retry via airplane mode, both-load-calls, one-shot pre-select, superseded load, reconnect refresh): network/lifecycle resilience — need airplane-mode toggling and backgrounding scripting; not driven this session.
- **E11/E12** (same-JC accumulation / cross-JC blocked): need a **second job card's** finished mixes. Only 510019068 is loadable in the sim seed (510018531 is not bopotStandard), so cross-JC material wasn't available without more setup.
- **E13** (Clear): the Clear button was exercised in E22/E23 setup (it dropped a selection), but an isolated verdict wasn't recorded.
- **E16–E20** (scan-beats-highlighting, scan-blocked-over-sheet, Rajoo dose sheet & validation): Rajoo cases need a **planned Rajoo collection**; not set up.
- **E24** (no-response re-sync) and the **[sim] fault-injection blocks** (§4.1b, §4.4c): need sim support to withhold/mangle a reply; the current sim has no fault-injection switch.
- **E26 / E-FC** (force-close on device): need an **active mixer cycle** at test time; all cycles were finished/assigned by the time role-gating was reached. NOTE: the **sim now implements** force-close quarantine + plan-item restore (see TASK 1), and force-close approval is covered by the selftest — but the on-device force-close UI path (an AlertDialog + manager credential entry) was not driven.

## Environment note (end of session)

The backend-sim and sniffer background processes exited (exit 1) at the very end of the session, **after** all test evidence was captured. The sim's log ends cleanly on the successful `COL_000004` resume at `09:19:51.473Z` (`res/bom_loaded ACCEPTED`) with **no error or traceback** — a clean exit (WSS loop returned), not a crash during a test. The sim was demonstrably alive and responding for every recorded verification (destination-assignment fix `09:08:03`, the whole mixing E-block, ingredient scans, pallet lookups `09:17`, manager resume `09:19:51`). `capture/wire.jsonl` holds 162 frames through `09:19:51`. `selftest.py --direct` (in-process sim) re-confirmed **149/149** at session end. Restart `python -u tools/backend-sim/sim.py` + `python -u tools/test-harness/sniffer.py docs/test-runs/2026-07-27/capture` to resume live testing.

---

# ADDENDUM — Session 3 (2026-07-27, ~11:30–): remaining sim-backed Gate 4 blocks

**Backend:** fresh backend-sim this session (fault-injection build) on `mqtt.sysone.co.za:443` WSS TLS; live Station 2 offline. **Device:** `HC720DE260100322`, app pill **Connected**. **Sniffer:** `capture/wire.jsonl`.

**Sim/harness changes made this session (selftest stayed green at 149 the whole way):**
- `tools/backend-sim/sim.py` — added an opt-in **fault-injection control plane** on topic `PPNAM/_sim/control` (guarded; no faults armed ⇒ zero behavioural change, so `--direct` selftest is unaffected). Commands: `presence` (A9), `withhold` (A14/A15/E24), `malformed` (A17), `uncorrelated` (A16), `reject` with `session:current|old` + `errorCode` (A18/A19/A20/D32), `login_mangle` mode `blank_session|session_closed|bad_expiry` (B3/B4/B5), `reset`.
- `tools/backend-sim/handlers/jobcards.py::seed_demo_collections` — appended two seeded ReadyForMixing collections (ids unchanged for COL_000001–003): **COL_000004** (510019068, plan reserves **RAJ-GM-01** — E16–E20) and **COL_000005** (510018531, plan reserves **MXR-03** — a 2nd-JC ready collection for E11/E12).
- `tools/test-harness/pallets_sim.json` — new sim-matched pallet fixture generated from `backend-sim/seed` (material → usable-Holding RFID tag).
- `tools/test-harness/collect_sim.py` — new sim-matched collection driver (arms a line, injects the sim's real tag as a barcode pallet stand-in, accepts the app's round-up prefill on bag lines / types exact kg on the bulk line, confirms; reads BOM + results from the sniffer wire). Staged one-line-per-call so the UI script never hangs.
- `tools/test-harness/simctl.py` — new helper to publish control frames to the sim (used by the fault blocks below).

## Block 1 — Full ingredient collection to ReadyForMixing (§4.4, D-block + D-INT)

Drove a **fresh** load of JC 510019068 (→ **COL_000006**) line-by-line to **ReadyForMixing** on device. All 7 manual BOM lines collected; on the final line the app **auto-navigated to the mixing area picker** with the banner **"COL_000006 ready to mix — pick an area"**.

| ID | Test | Verdict | Evidence |
|---|---|---|---|
| §4.4 | Full collection → ReadyForMixing | **PASS** | COL_000006 all 7 lines satisfied; `collectionStatus` → **ReadyForMixing** on the wire; 7 `ingredient_scan_requested` accepted |
| D5 | Tap to arm | **PASS** | tapping "Line N" arms it (ARMED badge); scan then opens the dialog |
| D7 | Correct dialog per type | **PASS** | bag lines → **"Bag size & count"**; bulk line 2 (1600000217) → **"Weight received"** |
| D8 | Round-up pre-fill | **PASS** | line 0 needs 2.79 bags → **Full bags pre-filled "3"**, fraction 0 |
| D10 | Dialog carries context | **PASS** | "Line 0 · MASTERBATCH…", "Still required: 2.79 bags of 25.000 kg", round-up rule, "Pallet: 300833B2DDD9014000000009" |
| D13 | Fractions offered | **PASS** | chips exactly `0, 1/4, 1/2, 3/4` |
| D14 | Weight dialog | **PASS(partial)** | bulk line uses "Weight received" / "Quantity (kg)" / **Confirm Weight**; positive weight accepted. Zero-refusal sub-case not separately driven |
| D19/D21/D22 | Progress / line replace / arming clears | **PASS** | "N of 7 lines satisfied" increments per accept; each line flips to "Fully Allocated"; backflush line 7 never appears as collectible |
| D23 | Auto-navigate on ready | **PASS** | final line → area picker, collection pre-selected |
| D24 | Approval prompt (over-tolerance) | **PASS** (bonus) | a deliberate 1672 kg on bulk line 2 (0.853 kg over, zero bulk tolerance) → **"Manager or admin approval required"** dialog carrying the server's verbatim reason; fields Username/Password/Audit reason + Cancel/Approve. **This dialog IS uiautomator-visible/drivable** (contradicts the earlier report note — only the force-close AlertDialog is a separate window) |
| D42 | Start-Mixing gating | **PASS** | "Mixing after collection" disabled while Collecting; replaced by the ready-to-mix flow at ReadyForMixing |
| D3 | Unambiguous auto-arm | **NOT RUN** | every line was tap-armed; single-unsatisfied-line auto-arm not isolated this session |
| D4 | Satisfied single-line rescan | **NOT RUN** | needs a single-line job card (none in seed) |
| D11/D12 | Short/zero refused | **NOT RUN** | dialog-validation sub-cases not driven (mechanism proven; would need extra device passes) |
| D20 | Scan-guard matrix | **NOT RUN** | 10-state matrix not driven this session |
| D33 | Rejection is clean | **PASS** (observed via D24 path) | bulk over-collection rejected → approval dialog over an intact line list; on Cancel, list unchanged, scanning resumed |

**D-INT (bag-math integrity) — PASS on the sim seed.** For every bag line, BOM `bagSize` (25.000 kg) == capturedKg ÷ scannedBags (25.0). Line 2 is bulk (direct weight, no bag math). No mismatch: the historical `1600000044` 20-vs-25 defect lives only in the harness `pallets.json` fixture, not in the sim seed.

| line | material | BOM bagSize | sent | weightReceived | scannedBags | kg/bag |
|---|---|---|---|---|---|---|
| 0 | 1500000326 | 25.000 kg | full×3 | 75.0 | 3.0 | 25.0 |
| 1 | 1600000301 | 25.000 kg | full×23 | 575.0 | 23.0 | 25.0 |
| 2 | 1600000217 | (bulk) | qty 1671.147 | 1671.147 | — | bulk |
| 3 | 1600000070 | 25.000 kg | full×23 | 575.0 | 23.0 | 25.0 |
| 4 | 1600000233 | 25.000 kg | full×12 | 300.0 | 12.0 | 25.0 |
| 5 | 1600000309 | 25.000 kg | full×23 | 575.0 | 23.0 | 25.0 |
| 6 | 1500000331 | 25.000 kg | full×1 | 25.0 | 1.0 | 25.0 |

## Block 5 — Sim fault-injection → §4.1b (A9–A20), B3–B5 (all driven on device via `PPNAM/_sim/control`)

All faults armed with `simctl.py`, triggered on device, verified on the wire + screen. `selftest --direct` stayed at **149** (faults default off).

| ID | Test | Verdict | Evidence |
|---|---|---|---|
| A9 | Garbage presence = offline | **PASS** | `presence maybe` → sim retains `maybe` on `PPNAM/station_2/status`; app pill flips to **"Station 2 offline"**; `reset` → back to Connected. Shot `A9-station-offline.png` |
| A14 | Replay identity | **PASS** | `withhold mixing_overview_requested` → sniffer shows **exactly 3** `mixing_overview_requested` frames, identical **messageId `9272c615…`** and identical body `timestampUtc 10:03:35.610800Z`, 20 s apart (10:03:35/:55, 10:04:15) |
| A15 | Timeout budget | **PASS** | after the 3rd attempt times out (~60 s total) the board shows **"Station 2 did not respond"** + Retry. Shot `A15-giveup.png` |
| A16 | Correlation discipline | **PASS** | `uncorrelated` → sim emits `S2-UNCORR-1` (no inResponseToMessageId) and `S2-UNCORR-2` (unknown id) **before** the real reply; app drops both and the board loads from the real response `S2-000005`. Wire confirms all three frames |
| A17 | Malformed response | **PASS** | `malformed` → sim emits a correlated frame whose body fails DTO parse (`S2-MALFORMED inResponseTo=bf20e…`); board shows **"Station 2 sent an unreadable response"** + Retry; app stays usable. Shot `A17-malformed2.png`. (Fixing note: a *fully* broken frame is dropped as uncorrelated → the request just times out; the app surfaces MalformedResponse only for a frame that correlates but fails the body DTO parse, matching `MqttRequestCorrelationTest`.) |
| A18 | Session loss logs out | **PASS** | `reject … session_required current` → app returns to **Login**. Shot `A18-session-logout.png` |
| A19 | Stale rejection does NOT log out | **PASS** | `reject … session_required old` → app **stays put** (operator still shown, board error "Injected fault: session_required" + Retry), no nav to Login. Shot `A19-stale-session-stays.png` |
| A20 | Upgrade gate blocks everything | **PASS** | `reject … client_upgrade_required` → **"App update required"** dialog ("…requires the 4.0 reader build…"); **survives Back** (not dismissible). Shot `A20-upgrade-gate.png` |
| B3 | Blank operatorSessionId | **PASS** | `login_mangle blank_session` → **"Station 2 accepted the login but issued no session"**; stays on Login. Shot `B3-blank-session.png` |
| B4 | sessionState Closed | **PASS** | `login_mangle session_closed` → **"Station 2 closed this session immediately"**; stays on Login. Shot `B4-session-closed.png` |
| B5 | Unparseable expiry | **PASS** | `login_mangle bad_expiry` → login **succeeds** (lands on Active Jobs). Shot `B5-bad-expiry.png` |
| A11 | No badge flicker (debounce) | **NOT RUN** | needs a paired delayed/corrected skew reply; not driven |

## Block 2 — Rajoo dose sheet (E16–E20) via seeded COL_000004 (plan reserves RAJ-GM-01)

Selecting COL_000004 highlights the reserved **RAJ-GM-01** (blue border, "Reserved"); RAJ-GM-02/03 stay Available (Rajoo E9). Tapping the highlighted Rajoo mixer opens the **"Start Rajoo Gravimetric Mixer Layer 1"** dose sheet (an AlertDialog — coordinate-driven).

**Sim fix required + made:** the dose sheet is built from `useCase.fetchCollectedMaterials(...)`, which resumes the collection to read its collected lines. The sim's `resume` handler rejected a **MixingPlanned** collection ("nothing to resume"), so the sheet could not open. Fixed `jobcards.py::resume` to treat MixingPlanned like ReadyForMixing (resumable → `start_mixing`). This is a sim fidelity gap I introduced with MixingPlanned, **not an app defect**. `selftest --direct` still **149**.

| ID | Test | Verdict | Evidence |
|---|---|---|---|
| E18 | Rajoo dose rows | **PASS** | one row per collected material, each **capped at its collected quantity**: MASTERBATCH (≤ 69.63), HD WHITE (≤ 557.05), LD MIX (≤ 1671.15), FILLER (≤ 557.05), + PALLET WRAP/STRETCHHOOD/DESICCANT. Shot `E18-dose-sheet.png` |
| E19 | empty submit | **PASS** | Start with 0 doses → red **"Enter at least one dose."** Shot `E19-empty.png` |
| E19 | above collected | **PASS** | 100 in MASTERBATCH (cap 69.63) → red **"A dose cannot exceed the collected quantity."** Shot `E19-above-collected.png` |
| E20 | dose counter | **PASS (partial)** | header reads **"0 / 5 doses entered"** → **"1 / 5"** after one entry (the cap-at-5 counter). The "5/5 + 6th field disabled" and E19 negative/6-row messages were **not fully driven** — the fixed-coordinate multi-field entry inside the invisible AlertDialog is fragile, and negative/6-row inputs are keyboard/cap blocked (covered by the app unit test `rajoo confirm validates doses fail-closed`) |
| E16/E17 | scan beats highlighting / scan over sheet | **NOT RUN** | not driven this session |

## Block 4 — Force-close + credential dialogs (E26 / E-FC), and a dialog-visibility correction

Drove a full mixer cycle then force-closed it as a manager, on device, against the sim.

| ID | Test | Verdict | Evidence |
|---|---|---|---|
| E14 | Tap highlighted mixer → Start confirm | **PASS** | select COL_000002, tap reserved MXR-01 → **"Start Main Mixer 1"** dialog. Shot `E14-start-confirm.png` |
| E21/E22 | Start + accepted refresh | **PASS** | Start → one `machine_cycle_start_requested MXR-01` → `Started CYC_000001 MIX_000001`; MXR-01 flips **InUse** |
| E15 | Tap busy → cycle sheet | **PASS** | tapping InUse MXR-01 → **"Active cycle on Main Mixer 1"** (Cycle CYC_000001, Mixes MIX_000001, started by OP-001) |
| E26 | Force close — operator absent / manager present | **PASS** | **as operator1** the cycle sheet shows only Cancel / Finish cycle — **no Force close** (shot `E26-operator-cyclesheet.png`); **as manager1** a red **"Force close…"** appears (shot `E26-manager-cyclesheet.png`) |
| E26 | Force close executes + names approver | **PASS** | **"Force close CYC_000001"** dialog ("Requires Manager/Admin approval; the cycle is released without completing.") → manager1/secret + audit reason "line jam force close" → **Force close** → wire `machine_cycle_force_close_requested MXR-01/CYC_000001` → `ForceClosed forceClosed=true` **approverDisplayName="Manager One"**. Shots `E26-forceclose-dialog.png`, `E26-forceclose-filled.png` |
| E-FC | Quarantined mix must NOT flow downstream | **PASS** | after force-close, `mixing_overview_result.readyMixes` = **0** (MIX_000001 absent from both the fresh overview and the force-close response's embedded areaStatus); MXR-01 plan item restored to **Reserved / scanAllowed=true** so the mixer can be re-scanned while the quarantined mix is never assignable. Sim's `assign_destinations` rejects a Quarantined mix (state_conflict), covered by selftest |

**Dialog-visibility correction (harness fact update):** the earlier report note said the credential AlertDialogs (force-close, approval, waiver, cancel) are invisible to `uiautomator dump`. In this run the **approval dialog** (D24) and the **force-close dialog** were both fully present in the uiautomator tree (labelled EditText fields + Cancel/confirm buttons) and drivable by dumped coordinates — only the **Start/Assign confirm** AlertDialogs and the **Rajoo dose sheet** truly render off-tree and need screenshot-coordinate driving. Fields still bleed across each other if the IME is left open between them, so close the IME between fields.

**Not driven (credential dialogs):** D25–D28 (approval resubmit / blank-refused / no-double / cancel-kills) and D34–D41 (waiver) beyond D24 — the mechanism is proven (D24 + force-close both driven), but the individual sub-cases were not each exercised this session.

## Block 3 — Second JC for cross-mix cases (E11 / E12)

Built the fixtures on device: start+finish MXR-02 → MIX_000002, MXR-01 → MIX_000003 (both JC 510019068); MXR-03 on COL_000005 → MIX_000004 (JC 510018531). All start/finish frames accepted on the wire.

| ID | Test | Verdict | Evidence |
|---|---|---|---|
| E11 | Same-JC accumulation | **PASS** | assigned MIX_000002 → EXT-01 (`mix_destination_assignment_result` → RUN_000001). Then selecting the second **same-JC** mix MIX_000003 keeps EXT-01 **highlighted** (blue border), i.e. it still accepts more into its run. Shot `E11-accumulation.png`. **Sim note (not an app defect):** `assign_destinations` does not mark the production machine InUse, so EXT-01 also reads "Available"; the active run (RUN_000001, same JC) is on the wire and the app's `computeHighlightedMachines` adds it via the accumulation path |
| E12 | Cross-JC blocked | **PASS** | with MIX_000003 (JC 510019068) selected, tapping the ready mix MIX_000004 (JC 510018531) is **non-interactive** — the selection stays exactly `MIX_000003` (the other-JC mix is greyed, tap does nothing). Shot `E12-crossjc.png`. Server also enforces it: `assign_destinations` rejects mixed-JC sets with `job_card_mismatch` (selftest) |
| E14/E21/E22/E25 | start / no-double / accepted refresh / finish | **PASS** | exercised repeatedly building the fixtures (5 clean start/finish cycles on the wire) |
| E9 (bonus) | Collection highlights its reserved mixers | **PASS** | selecting COL_000005 highlights **only** its reserved MXR-03 (after a Refresh); a first attempt showed MXR-03 un-highlighted from a **stale board** — a Refresh corrected it (not an app defect; confirmed on the wire MXR-03 scanAllowed=true/reservationCollectionId=COL_000005) |
| E13 | Clear selection | **PASS** (observed) | Clear drops the active selection throughout the run |
