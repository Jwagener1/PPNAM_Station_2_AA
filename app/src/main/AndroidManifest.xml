<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".PpnamApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PPNAMStation2AA">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PPNAMStation2AA">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".data.rfid.DataWedgeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.ppnam.station2aa.ACTION_SCAN" />
                <action android:name="com.scanner.broadcast" />
                <action android:name="com.rscja.scanner.action.scanner.RFID" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
